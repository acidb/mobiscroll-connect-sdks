# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# parallel 2.x requires Ruby 3.3; pin to 1.x so the Ruby 3.2 CI job can install dev deps.
# Pulled in transitively by rubocop.
gem 'parallel', '< 2'
