# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# public_suffix 7+ requires Ruby >= 3.2; pin to keep Ruby 3.1 supported.
# Pulled in transitively by webmock (dev) via addressable.
gem 'public_suffix', '< 7'
